name: build-deploy

on:
  push:
    branches:
      - 'master'

jobs:
  deploy_web:
    runs-on: surcloud
    container:
      image: docker/compose:latest
    steps:
      - run: |
          chmod u+x build-run.sh
          ./build-run.sh